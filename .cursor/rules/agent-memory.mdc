---
description: 每次对话前检索 .agents-history，回答后在当日文件夹下新建单条 Q&A 文件
alwaysApply: true
---

# Agent 对话记忆（每次执行）

**存放结构**：一天一个文件夹 `.agents-history/YYYY-MM-DD/`，内部**每次提问一个文件**，便于按条检索、控制注入 token（只读命中的单条文件，不读整日大文件）。若存在旧版「单日单文件」如 `YYYY-MM-DD.md`，检索时也可 grep 命中后只读匹配的块，新对话一律写入当日文件夹下的新文件。

## 回答前

- **强制第一步**：每次回答用户问题前，必须先对 `.agents-history/` 执行一次 **grep 检索**，用当前问题中的关键词/主题（如模块名、接口名、功能名）作为检索词，不可跳过。
- 在 `.agents-history/` 下按关键词/主题 grep（会命中各日期子目录下的 .md 文件）。
- **只读取与问题匹配的文件**（每个文件即一条 Q&A），作为上下文；不读取整日或全部历史，避免浪费 token。
- 若有相关文件，再读取其内容后回答；无相关则可不读。
- 检索到多条时，优先采用日期较新、主题更贴合的条目。
- 检索示例：问「之前 Matter 适配怎么做的」→ grep `Matter` 或 `matter`，只读命中文件；问「对话历史怎么实现的」→ grep `历史` 或 `对话记忆`，只读命中文件。

## 回答后（不可省略）

- **强制第二步**：每次回答完用户问题后，必须在**同一轮回复内**在 `.agents-history/YYYY-MM-DD/`（今日日期）下**新建一个文件**记录本条 Q&A，不要追加到已有文件，不可跳过。目录不存在则先创建。
- **文件名**：用**提问的简短摘要或关键词**（与文件内 Q 一致或缩略），便于按文件名直接检索。例如 `videoSpace重新扫描设备列表更新.md`、`Agent记忆规则token消耗.md`。要求：去掉或替换非法字符（`/ \ : * ? " < > |` 等）为 `-` 或删除；过长可截断（建议 40 字内）；若与当日已有文件名重复则加 `-1`、`-2` 区分。
- 文件内容为**单条记录**（仅本条 Q&A），见下方格式。

### 单条记录格式（每文件仅一条）

每个文件内只放一块，结构：

```markdown
### Q: [问题摘要]

- **答**：[回答要点]
- 时间：YYYY-MM-DD HH:mm
```

- **Q**：一行或两行概括用户问题，保留关键词（便于 grep 命中）。
- **答**：以「能支撑后续同主题追问」为准，不必刻意压缩过短；建议**首句结论 + 关键要点/步骤**，可含文件路径、函数名、接口名等，便于日后复用。
- **时间**：可选。

### 答的写法（避免过简）

- **结论句**：说明「做了什么 / 原因是什么 / 结论是什么」。
- **关键要点**：可包含涉及文件、关键函数/接口、流程步骤或条件；技术类建议保留**路径 + 关键符号名**，方便后续「接着改」时直接定位。

### 示例

```markdown
### Q: videoSpace 重新扫描后设备列表如何更新？

- **答**：列表由轮询 getDeviceList 的返回直接更新。流程：handleRefresh → spaceInfoList(true) → getStateMark + setMqtt → setDelay(1000) 启动 useInterval；每 1 秒 newGetDeviceList() 调 getDeviceList('video', true)，当 res.finish 为 true 时 setDeviceList(newList)。涉及：videoSpace.tsx、api getDeviceList/getStateMark。
- 时间：2025-03-06
```
