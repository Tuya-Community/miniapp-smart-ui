---
description: 强制执行对话记忆：回答前检索历史，回答后存入当日原子化文件
alwaysApply: true
---

# Agent 记忆规则 (Memory Protocol)

**存储结构**：`.agents-history/YYYY-MM-DD/提问摘要.md`
**日期基准**：严格采用上下文提供的 `Today's date`，严禁跨天写入。

## 1. 回答前：精准检索 (Required)

- **动作**：通过 `grep` 检索 `.agents-history/` 目录下与当前问题相关的关键词（模块名、接口、功能）。
- **策略**：**只读取**命中的单条文件（原子化读取），不读整日大文件。
- **优先级**：日期较新 > 主题相关性。
- **目的**：获取历史背景（如：Matter 适配进度、对话逻辑），减少重复确认。

## 2. 回答后：原子记录 (Required)

- **动作**：每次回复结束后，在当日文件夹新建一个 `.md` 文件记录本条 Q&A。
- **命名**：`[简短摘要/关键词].md`（限 40 字，过滤非法字符，重复则加后缀 `-1`）。
- **内容格式**：
  ```markdown
  ### Q: [问题摘要/核心关键词]

  - **答**：[结论句 + 关键路径/函数名 + 流程要点]。确保能支撑后续追问。
  - 时间：YYYY-MM-DD HH:mm
  ```
