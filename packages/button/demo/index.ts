import { PlusCircle as iconAdd } from '@tuya-miniapp/icons';
import { Right as iconRight } from '@tuya-miniapp/icons';
import { SmartComponent } from '../../common/component';

SmartComponent({
  data: {
    iconAdd,
    iconRight,
  },
});
