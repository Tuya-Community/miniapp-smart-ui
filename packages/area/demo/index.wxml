<demo-block title="{{I18n.t('basicUsage')}}">
  <smart-area
    value="{{ value }}"
    loading="{{ loading }}"
    area-list="{{ areaList }}"
    bind:change="onChange"
    bind:confirm="onConfirm"
    bind:cancel="onCancel"
  />
</demo-block>
<smart-toast id="smart-toast-area" />
